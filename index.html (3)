<!DOCTYPE html>
<html lang="nl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Satine — Satijnen Self-Care</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;1,400&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>

/* ── RESET & BASE ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; font-size: 16px; }
body {
  font-family: 'DM Sans', sans-serif;
  font-weight: 400;
  background: #F8F4EF;
  color: #1C1610;
  -webkit-font-smoothing: antialiased;
}
img { display: block; width: 100%; object-fit: cover; }
a { text-decoration: none; color: inherit; }
button { font-family: inherit; cursor: pointer; }

/* ── VARIABLES ── */
:root {
  --ink:      #1C1610;
  --cream:    #F8F4EF;
  --blush:    #EFE2D5;
  --warm:     #E0CBBА;
  --sand:     #D4BFA8;
  --muted:    #9A8878;
  --accent:   #B8896A;
  --gold:     #C4A882;
  --white:    #FFFDF9;
  --border:   rgba(28,22,16,0.10);
  --border-s: rgba(28,22,16,0.18);
}

/* ── ANNOUNCEMENT BAR ── */
.announcement {
  background: var(--ink);
  text-align: center;
  padding: 10px 2rem;
}
.announcement p {
  font-size: 11px;
  letter-spacing: 2.5px;
  color: rgba(255,253,249,0.55);
  text-transform: uppercase;
}
.announcement span { color: var(--gold); }

/* ── NAV ── */
nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: var(--cream);
  border-bottom: 1px solid var(--border);
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: 0 3rem;
  height: 68px;
}
.nav-left {
  display: flex;
  gap: 2.5rem;
  align-items: center;
}
.nav-left a, .nav-right a {
  font-size: 11px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--muted);
  transition: color 0.2s;
}
.nav-left a:hover, .nav-right a:hover { color: var(--ink); }
.nav-logo {
  font-family: 'Playfair Display', serif;
  font-size: 26px;
  font-weight: 400;
  letter-spacing: 10px;
  color: var(--ink);
  text-align: center;
  padding-right: 10px;
}
.nav-right {
  display: flex;
  gap: 2.5rem;
  align-items: center;
  justify-content: flex-end;
}

/* ── HERO ── */
.hero {
  display: grid;
  grid-template-columns: 1fr 1fr;
  height: 92vh;
  min-height: 580px;
}
.hero-left {
  background: var(--ink);
  padding: 5rem 4rem;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  position: relative;
  overflow: hidden;
}
.hero-left::before {
  content: 'S';
  font-family: 'Playfair Display', serif;
  font-size: 420px;
  font-weight: 400;
  color: rgba(255,253,249,0.03);
  position: absolute;
  bottom: -60px;
  right: -40px;
  line-height: 1;
  pointer-events: none;
  user-select: none;
}
.hero-eyebrow {
  font-size: 10px;
  letter-spacing: 4px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.8rem;
}
.hero-title {
  font-family: 'Playfair Display', serif;
  font-size: 68px;
  font-weight: 400;
  line-height: 1.05;
  color: var(--white);
  margin-bottom: 1.8rem;
  position: relative;
}
.hero-title em {
  font-style: italic;
  color: var(--gold);
}
.hero-body {
  font-size: 13px;
  line-height: 2;
  color: rgba(255,253,249,0.45);
  max-width: 300px;
  margin-bottom: 3rem;
}
.hero-right {
  background: var(--blush);
  padding: 5rem 4rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}
.hero-right-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}
.hero-right-label {
  font-size: 10px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--muted);
}
.hero-right-num {
  font-family: 'Playfair Display', serif;
  font-size: 11px;
  color: var(--muted);
}
.hero-right-center {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 3rem 0;
}
.hero-quote {
  font-family: 'Playfair Display', serif;
  font-size: 34px;
  font-weight: 400;
  font-style: italic;
  color: var(--ink);
  line-height: 1.4;
  margin-bottom: 1.5rem;
}
.hero-quote-sub {
  font-size: 12px;
  color: var(--muted);
  letter-spacing: 0.5px;
  line-height: 1.8;
}
.hero-right-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 2rem;
  border-top: 1px solid var(--border);
}
.hero-stat { text-align: center; }
.hero-stat-num {
  font-family: 'Playfair Display', serif;
  font-size: 28px;
  font-weight: 400;
  color: var(--ink);
  display: block;
}
.hero-stat-label {
  font-size: 10px;
  letter-spacing: 1.5px;
  color: var(--muted);
  text-transform: uppercase;
}

/* ── BUTTONS ── */
.btn {
  display: inline-block;
  font-size: 10px;
  letter-spacing: 2.5px;
  text-transform: uppercase;
  padding: 14px 36px;
  transition: all 0.25s;
  cursor: pointer;
  border: none;
}
.btn-ghost {
  background: transparent;
  border: 1px solid rgba(255,253,249,0.25);
  color: var(--white);
}
.btn-ghost:hover {
  background: rgba(255,253,249,0.08);
  border-color: rgba(255,253,249,0.5);
}
.btn-solid {
  background: var(--ink);
  color: var(--white);
  border: 1px solid var(--ink);
}
.btn-solid:hover { background: #2d241e; }
.btn-cream {
  background: transparent;
  border: 1px solid var(--border-s);
  color: var(--ink);
}
.btn-cream:hover { background: var(--ink); color: var(--white); border-color: var(--ink); }

/* ── TICKER STRIP ── */
.ticker {
  background: var(--ink);
  padding: 14px 0;
  display: flex;
  gap: 0;
  overflow: hidden;
}
.ticker-inner {
  display: flex;
  gap: 4rem;
  animation: ticker 20s linear infinite;
  white-space: nowrap;
}
@keyframes ticker {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}
.ticker-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-shrink: 0;
}
.ticker-dot {
  width: 3px;
  height: 3px;
  border-radius: 50%;
  background: var(--gold);
  flex-shrink: 0;
}
.ticker-text {
  font-size: 10px;
  letter-spacing: 3px;
  color: rgba(255,253,249,0.35);
  text-transform: uppercase;
}

/* ── PHILOSOPHY ── */
.philosophy {
  padding: 7rem 3rem;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 6rem;
  max-width: 1100px;
  margin: 0 auto;
  align-items: center;
}
.phil-label {
  font-size: 10px;
  letter-spacing: 4px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.5rem;
}
.phil-title {
  font-family: 'Playfair Display', serif;
  font-size: 44px;
  font-weight: 400;
  color: var(--ink);
  line-height: 1.2;
  margin-bottom: 2rem;
}
.phil-title em { font-style: italic; color: var(--accent); }
.phil-body {
  font-size: 13px;
  color: var(--muted);
  line-height: 2;
  margin-bottom: 2.5rem;
}
.phil-right { display: flex; flex-direction: column; gap: 2rem; }
.phil-item {
  padding: 1.8rem 2rem;
  border: 1px solid var(--border);
  background: var(--white);
  display: flex;
  gap: 1.5rem;
  align-items: flex-start;
  transition: border-color 0.2s;
}
.phil-item:hover { border-color: var(--sand); }
.phil-num {
  font-family: 'Playfair Display', serif;
  font-size: 32px;
  font-weight: 400;
  color: var(--gold);
  line-height: 1;
  flex-shrink: 0;
}
.phil-item-title {
  font-size: 13px;
  font-weight: 500;
  color: var(--ink);
  margin-bottom: 0.4rem;
  letter-spacing: 0.3px;
}
.phil-item-body {
  font-size: 12px;
  color: var(--muted);
  line-height: 1.8;
}

/* ── PRODUCTS ── */
.products {
  background: var(--white);
  padding: 6rem 3rem;
}
.section-header {
  max-width: 1100px;
  margin: 0 auto 3.5rem;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  border-bottom: 1px solid var(--border);
  padding-bottom: 1.5rem;
}
.section-label {
  font-size: 10px;
  letter-spacing: 4px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 0.5rem;
}
.section-title {
  font-family: 'Playfair Display', serif;
  font-size: 36px;
  font-weight: 400;
  color: var(--ink);
}
.section-link {
  font-size: 11px;
  letter-spacing: 1.5px;
  color: var(--muted);
  text-transform: uppercase;
  border-bottom: 1px solid var(--border-s);
  padding-bottom: 2px;
  transition: color 0.2s;
}
.section-link:hover { color: var(--ink); }

.pgrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  max-width: 1100px;
  margin: 0 auto;
  border-left: 1px solid var(--border);
  border-top: 1px solid var(--border);
}
.pc {
  border-right: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  padding: 2.5rem 2rem;
  display: flex;
  flex-direction: column;
  background: var(--white);
  transition: background 0.25s;
  position: relative;
}
.pc:hover { background: #FDFAF6; }
.pc-tag {
  font-size: 9px;
  letter-spacing: 2.5px;
  color: var(--gold);
  text-transform: uppercase;
  margin-bottom: 1.2rem;
}
.pc-name {
  font-family: 'Playfair Display', serif;
  font-size: 22px;
  font-weight: 400;
  color: var(--ink);
  margin-bottom: 0.7rem;
}
.pc-desc {
  font-size: 12px;
  color: var(--muted);
  line-height: 1.8;
  flex: 1;
  margin-bottom: 2rem;
}
.pc-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 1.2rem;
  border-top: 1px solid var(--border);
}
.pc-price {
  font-family: 'Playfair Display', serif;
  font-size: 22px;
  font-weight: 400;
  color: var(--ink);
}
.pc-btn {
  font-size: 9px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--muted);
  background: none;
  border: none;
  border-bottom: 1px solid var(--border-s);
  padding-bottom: 3px;
  transition: color 0.2s, border-color 0.2s;
}
.pc-btn:hover { color: var(--ink); border-color: var(--ink); }

/* ── UPSELL ── */
.upsell {
  max-width: 1100px;
  margin: 3rem auto 0;
  background: var(--ink);
  display: grid;
  grid-template-columns: 1fr auto;
  align-items: center;
  padding: 2.5rem 3rem;
  gap: 3rem;
}
.upsell-eyebrow {
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 0.5rem;
}
.upsell-title {
  font-family: 'Playfair Display', serif;
  font-size: 26px;
  font-weight: 400;
  color: var(--white);
  margin-bottom: 0.4rem;
}
.upsell-sub {
  font-size: 12px;
  color: rgba(255,253,249,0.35);
  line-height: 1.7;
}
.upsell-btns { display: flex; gap: 1rem; flex-shrink: 0; }

/* ── BUNDLES ── */
.bundles {
  padding: 7rem 3rem;
  background: var(--blush);
}
.bgrid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
  max-width: 1100px;
  margin: 0 auto;
}
.bc {
  background: var(--white);
  border: 1px solid var(--border);
  padding: 3rem 2.5rem;
  display: flex;
  flex-direction: column;
  transition: border-color 0.25s;
}
.bc:hover { border-color: var(--sand); }
.bc.feat {
  background: var(--ink);
  border-color: var(--ink);
}
.bc-eye {
  font-size: 9px;
  letter-spacing: 4px;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.2rem;
}
.bc.feat .bc-eye { color: rgba(255,253,249,0.3); }
.bc-name {
  font-family: 'Playfair Display', serif;
  font-size: 34px;
  font-weight: 400;
  color: var(--ink);
  margin-bottom: 0.6rem;
  line-height: 1.1;
}
.bc.feat .bc-name { color: var(--white); }
.bc-desc {
  font-size: 12px;
  color: var(--muted);
  line-height: 1.8;
  margin-bottom: 2rem;
  min-height: 44px;
}
.bc.feat .bc-desc { color: rgba(255,253,249,0.35); }
.bc-divider {
  height: 1px;
  background: var(--border);
  margin-bottom: 1.5rem;
}
.bc.feat .bc-divider { background: rgba(255,253,249,0.08); }
.bc-items { flex: 1; margin-bottom: 2rem; }
.bc-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-bottom: 1px solid var(--border);
}
.bc.feat .bc-item { border-color: rgba(255,253,249,0.07); }
.bc-item-name {
  font-size: 12px;
  color: var(--muted);
}
.bc.feat .bc-item-name { color: rgba(255,253,249,0.45); }
.bc-item-line {
  width: 20px;
  height: 1px;
  background: var(--gold);
  flex-shrink: 0;
}
.bc-footer {}
.bc-price-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 1.5rem;
}
.bc-price {
  font-family: 'Playfair Display', serif;
  font-size: 38px;
  font-weight: 400;
  color: var(--ink);
}
.bc.feat .bc-price { color: var(--white); }
.bc-save {
  font-size: 11px;
  color: var(--gold);
  letter-spacing: 0.5px;
}
.bc-btn {
  width: 100%;
  padding: 15px;
  font-size: 10px;
  letter-spacing: 3px;
  text-transform: uppercase;
  font-family: 'DM Sans', sans-serif;
  font-weight: 400;
  cursor: pointer;
  transition: all 0.25s;
}
.bc-btn-cream {
  background: transparent;
  border: 1px solid var(--border-s);
  color: var(--ink);
}
.bc-btn-cream:hover { background: var(--ink); color: var(--white); border-color: var(--ink); }
.bc-btn-ghost {
  background: transparent;
  border: 1px solid rgba(255,253,249,0.2);
  color: var(--white);
}
.bc-btn-ghost:hover { background: rgba(255,253,249,0.07); border-color: rgba(255,253,249,0.4); }

/* ── TRUST ── */
.trust {
  padding: 5rem 3rem;
  background: var(--white);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}
.trust-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  max-width: 1100px;
  margin: 0 auto;
  gap: 0;
}
.trust-item {
  text-align: center;
  padding: 2rem 1.5rem;
  border-right: 1px solid var(--border);
}
.trust-item:last-child { border: none; }
.trust-icon {
  width: 32px;
  height: 1px;
  background: var(--gold);
  margin: 0 auto 1.5rem;
}
.trust-title {
  font-family: 'Playfair Display', serif;
  font-size: 16px;
  font-weight: 400;
  color: var(--ink);
  margin-bottom: 0.5rem;
}
.trust-body {
  font-size: 11px;
  color: var(--muted);
  line-height: 1.8;
}

/* ── FOOTER ── */
footer {
  background: var(--ink);
  padding: 5rem 3rem 3rem;
}
.footer-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 4rem;
  max-width: 1100px;
  margin: 0 auto;
  padding-bottom: 3rem;
  border-bottom: 1px solid rgba(255,253,249,0.07);
}
.footer-brand {}
.footer-logo {
  font-family: 'Playfair Display', serif;
  font-size: 28px;
  font-weight: 400;
  letter-spacing: 8px;
  color: rgba(255,253,249,0.8);
  margin-bottom: 1rem;
}
.footer-tagline {
  font-size: 12px;
  color: rgba(255,253,249,0.25);
  line-height: 1.9;
  max-width: 220px;
}
.footer-col-title {
  font-size: 10px;
  letter-spacing: 3px;
  color: rgba(255,253,249,0.3);
  text-transform: uppercase;
  margin-bottom: 1.5rem;
}
.footer-links { list-style: none; }
.footer-links li {
  margin-bottom: 0.8rem;
}
.footer-links a {
  font-size: 12px;
  color: rgba(255,253,249,0.3);
  transition: color 0.2s;
}
.footer-links a:hover { color: rgba(255,253,249,0.7); }
.footer-bottom {
  max-width: 1100px;
  margin: 2rem auto 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.footer-copy {
  font-size: 10px;
  letter-spacing: 1.5px;
  color: rgba(255,253,249,0.18);
  text-transform: uppercase;
}

/* ── ANIMATIONS ── */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.7s ease, transform 0.7s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: translateY(0);
}

/* ── RESPONSIVE ── */
@media (max-width: 900px) {
  nav { grid-template-columns: 1fr auto; padding: 0 1.5rem; }
  .nav-left, .nav-right { display: none; }
  .hero { grid-template-columns: 1fr; height: auto; }
  .hero-left { padding: 4rem 2rem; min-height: 60vh; }
  .hero-right { padding: 3rem 2rem; }
  .hero-title { font-size: 48px; }
  .philosophy { grid-template-columns: 1fr; gap: 3rem; padding: 4rem 2rem; }
  .pgrid { grid-template-columns: repeat(2, 1fr); }
  .bgrid { grid-template-columns: 1fr; }
  .trust-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-grid { grid-template-columns: 1fr 1fr; gap: 2rem; }
  .upsell { grid-template-columns: 1fr; }
  .upsell-btns { flex-direction: column; }
  .section-header { flex-direction: column; align-items: flex-start; gap: 1rem; }
  .products, .bundles { padding: 4rem 1.5rem; }
}

@media (max-width: 540px) {
  .hero-title { font-size: 38px; }
  .pgrid { grid-template-columns: 1fr; }
  .trust-grid { grid-template-columns: 1fr; }
  .trust-item { border-right: none; border-bottom: 1px solid var(--border); }
  .footer-grid { grid-template-columns: 1fr; }
}

</style>
</head>
<body>

<!-- ANNOUNCEMENT -->
<div class="announcement">
  <p>Gratis verzending bij besteding vanaf €30 &nbsp;<span>·</span>&nbsp; Snel geleverd in Nederland &nbsp;<span>·</span>&nbsp; Premium satijn</p>
</div>

<!-- NAV -->
<nav>
  <div class="nav-left">
    <a href="#collectie">Collectie</a>
    <a href="#bundels">Bundels</a>
  </div>
  <a href="#" class="nav-logo">SATINE</a>
  <div class="nav-right">
    <a href="#over">Over ons</a>
    <a href="#contact">Contact</a>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-left">
    <p class="hero-eyebrow">Satijnen self-care</p>
    <h1 class="hero-title">Voor jezelf<br>zorgen,<br><em>zacht.</em></h1>
    <p class="hero-body">Premium satijnen accessoires voor elke ochtend en elke nacht. Ontworpen voor vrouwen die zichzelf serieus nemen.</p>
    <a href="#collectie" class="btn btn-ghost">Bekijk collectie</a>
  </div>
  <div class="hero-right">
    <div class="hero-right-top">
      <span class="hero-right-label">Satine — 2025</span>
      <span class="hero-right-num">01 / 02</span>
    </div>
    <div class="hero-right-center">
      <p class="hero-quote">"Elke nacht een<br>rituel. Elke ochtend<br>een statement."</p>
      <p class="hero-quote-sub">Satijn beschermt je haar, verzacht je huid<br>en voelt elke dag als een luxe.</p>
    </div>
    <div class="hero-right-bottom">
      <div class="hero-stat">
        <span class="hero-stat-num">100%</span>
        <span class="hero-stat-label">Satijn</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-num">2</span>
        <span class="hero-stat-label">Bundels</span>
      </div>
      <div class="hero-stat">
        <span class="hero-stat-num">NL</span>
        <span class="hero-stat-label">Gebaseerd</span>
      </div>
    </div>
  </div>
</section>

<!-- TICKER -->
<div class="ticker" aria-hidden="true">
  <div class="ticker-inner">
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Premium Satijn</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Gratis Verzending</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Snel Geleverd</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Veilig Betalen</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Self-Care Collectie</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Dagelijkse Luxe</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Premium Satijn</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Gratis Verzending</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Snel Geleverd</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Veilig Betalen</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Self-Care Collectie</span></div>
    <div class="ticker-item"><div class="ticker-dot"></div><span class="ticker-text">Dagelijkse Luxe</span></div>
  </div>
</div>

<!-- PHILOSOPHY -->
<section class="philosophy fade-in" id="over">
  <div>
    <p class="phil-label">Waarom Satine</p>
    <h2 class="phil-title">Satijn is meer<br>dan een <em>gevoel.</em></h2>
    <p class="phil-body">Satijn beschermt je haar tegen breuk en je huid tegen wrijving. Elke nacht en elke ochtend een kleine daad van zelfzorg, die je terugziet.</p>
    <a href="#collectie" class="btn btn-cream">Ontdek de collectie</a>
  </div>
  <div class="phil-right">
    <div class="phil-item">
      <span class="phil-num">01</span>
      <div>
        <p class="phil-item-title">Beschermt je haar</p>
        <p class="phil-item-body">Satijn veroorzaakt minder wrijving dan katoen, waardoor je haar minder breekt en soepeler blijft.</p>
      </div>
    </div>
    <div class="phil-item">
      <span class="phil-num">02</span>
      <div>
        <p class="phil-item-title">Verzacht je huid</p>
        <p class="phil-item-body">Het gladde oppervlak van satijn is zachter voor je huid en vermindert het achterlaten van lijntjes.</p>
      </div>
    </div>
    <div class="phil-item">
      <span class="phil-num">03</span>
      <div>
        <p class="phil-item-title">Dagelijkse luxe</p>
        <p class="phil-item-body">Premium kwaliteit die aanvoelt als een dagelijks rituel — niet als een accessoire.</p>
      </div>
    </div>
  </div>
</section>

<!-- PRODUCTS -->
<section class="products fade-in" id="collectie">
  <div class="section-header">
    <div>
      <p class="section-label">Collectie</p>
      <h2 class="section-title">Losse producten</h2>
    </div>
    <a href="#bundels" class="section-link">Bekijk bundels</a>
  </div>
  <div class="pgrid">
    <div class="pc">
      <p class="pc-tag">Self-care</p>
      <h3 class="pc-name">Slaapmutsje</h3>
      <p class="pc-desc">Bescherm je haar elke nacht. Zacht satijn voorkomt breuk en houdt je haar soepel.</p>
      <div class="pc-footer">
        <span class="pc-price">€12,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
    <div class="pc">
      <p class="pc-tag">Self-care</p>
      <h3 class="pc-name">Slaapmasker</h3>
      <p class="pc-desc">Zacht satijnen slaapmasker voor een diepe, ongestoorde nachtrust.</p>
      <div class="pc-footer">
        <span class="pc-price">€9,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
    <div class="pc">
      <p class="pc-tag">Haar</p>
      <h3 class="pc-name">Scrunchie set</h3>
      <p class="pc-desc">3 satijnen scrunchies in neutrale tinten. Zacht voor je haar, stijlvol voor elke dag.</p>
      <div class="pc-footer">
        <span class="pc-price">€8,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
    <div class="pc">
      <p class="pc-tag">Self-care</p>
      <h3 class="pc-name">Kussensloop</h3>
      <p class="pc-desc">Satijnen kussensloop voor haar én huid. Elke nacht zachter wakker worden.</p>
      <div class="pc-footer">
        <span class="pc-price">€19,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
    <div class="pc">
      <p class="pc-tag">Haar</p>
      <h3 class="pc-name">Haarband</h3>
      <p class="pc-desc">Brede satijnen haarband voor ochtend en avond. Comfortabel en stijlvol.</p>
      <div class="pc-footer">
        <span class="pc-price">€7,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
    <div class="pc">
      <p class="pc-tag">Haar</p>
      <h3 class="pc-name">Claw clips</h3>
      <p class="pc-desc">Satijnen haarklauwen in 4 neutrale kleuren. Grip zonder schade.</p>
      <div class="pc-footer">
        <span class="pc-price">€7,95</span>
        <button class="pc-btn">Bestel</button>
      </div>
    </div>
  </div>
  <div class="upsell">
    <div>
      <p class="upsell-eyebrow">Bundels</p>
      <p class="upsell-title">Combineer en bespaar tot €8,—</p>
      <p class="upsell-sub">Onze bundels zijn samengesteld voor de perfecte routine — en je betaalt minder dan los.</p>
    </div>
    <div class="upsell-btns">
      <a href="#bundels" class="btn btn-ghost">Nacht bundel — €24,95</a>
      <a href="#bundels" class="btn" style="background:var(--blush);color:var(--ink);border:1px solid var(--blush);">Ochtend bundel — €19,95</a>
    </div>
  </div>
</section>

<!-- BUNDLES -->
<section class="bundles fade-in" id="bundels">
  <div class="section-header" style="margin-bottom:3.5rem">
    <div>
      <p class="section-label">Bundels</p>
      <h2 class="section-title">Alles in één</h2>
    </div>
  </div>
  <div class="bgrid">
    <div class="bc feat">
      <p class="bc-eye">Nacht</p>
      <h3 class="bc-name">Nacht<br>Bundel</h3>
      <p class="bc-desc">Jouw complete satijnen nachtroutine in één pakket.</p>
      <div class="bc-divider"></div>
      <div class="bc-items">
        <div class="bc-item"><span class="bc-item-name">Satijnen slaapmutsje</span><span class="bc-item-line"></span></div>
        <div class="bc-item"><span class="bc-item-name">Satijnen slaapmasker</span><span class="bc-item-line"></span></div>
        <div class="bc-item"><span class="bc-item-name">Scrunchie set (3x)</span><span class="bc-item-line"></span></div>
      </div>
      <div class="bc-footer">
        <div class="bc-price-row">
          <span class="bc-price">€24,95</span>
          <span class="bc-save">Bespaar €6,90</span>
        </div>
        <button class="bc-btn bc-btn-ghost">Bestel bundel</button>
      </div>
    </div>
    <div class="bc">
      <p class="bc-eye">Ochtend</p>
      <h3 class="bc-name">Ochtend<br>Bundel</h3>
      <p class="bc-desc">Begin elke dag goed verzorgd met de juiste basis.</p>
      <div class="bc-divider"></div>
      <div class="bc-items">
        <div class="bc-item"><span class="bc-item-name">Satijnen haarband</span><span class="bc-item-line"></span></div>
        <div class="bc-item"><span class="bc-item-name">Claw clips (4x)</span><span class="bc-item-line"></span></div>
        <div class="bc-item"><span class="bc-item-name">Scrunchie set (3x)</span><span class="bc-item-line"></span></div>
      </div>
      <div class="bc-footer">
        <div class="bc-price-row">
          <span class="bc-price">€19,95</span>
          <span class="bc-save">Bespaar €4,90</span>
        </div>
        <button class="bc-btn bc-btn-cream">Bestel bundel</button>
      </div>
    </div>
  </div>
</section>

<!-- TRUST -->
<div class="trust fade-in">
  <div class="trust-grid">
    <div class="trust-item">
      <div class="trust-icon"></div>
      <p class="trust-title">Gratis verzending</p>
      <p class="trust-body">Bij besteding vanaf €30 gratis bezorgd in heel Nederland.</p>
    </div>
    <div class="trust-item">
      <div class="trust-icon"></div>
      <p class="trust-title">Snel geleverd</p>
      <p class="trust-body">Besteld voor 22:00, de volgende werkdag in huis.</p>
    </div>
    <div class="trust-item">
      <div class="trust-icon"></div>
      <p class="trust-title">Premium kwaliteit</p>
      <p class="trust-body">Zorgvuldig geselecteerd satijn dat zacht aanvoelt en lang meegaat.</p>
    </div>
    <div class="trust-item">
      <div class="trust-icon"></div>
      <p class="trust-title">Veilig betalen</p>
      <p class="trust-body">Betaal veilig via iDeal, creditcard of PayPal.</p>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer id="contact">
  <div class="footer-grid">
    <div class="footer-brand">
      <p class="footer-logo">SATINE</p>
      <p class="footer-tagline">Satijnen self-care accessoires voor elke dag. Premium kwaliteit, dagelijkse luxe.</p>
    </div>
    <div>
      <p class="footer-col-title">Collectie</p>
      <ul class="footer-links">
        <li><a href="#">Losse producten</a></li>
        <li><a href="#">Nacht Bundel</a></li>
        <li><a href="#">Ochtend Bundel</a></li>
      </ul>
    </div>
    <div>
      <p class="footer-col-title">Informatie</p>
      <ul class="footer-links">
        <li><a href="#">Over ons</a></li>
        <li><a href="#">Verzending</a></li>
        <li><a href="#">Retourneren</a></li>
      </ul>
    </div>
    <div>
      <p class="footer-col-title">Contact</p>
      <ul class="footer-links">
        <li><a href="mailto:info@satine.nl">info@satine.nl</a></li>
        <li><a href="#">Instagram</a></li>
        <li><a href="#">TikTok</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p class="footer-copy">© 2025 Satine — Alle rechten voorbehouden</p>
    <p class="footer-copy">KVK 12345678 &nbsp;·&nbsp; info@satine.nl</p>
  </div>
</footer>

<script>
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
    }
  });
}, { threshold: 0.1 });
document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
</script>

</body>
</html>
